<h3>Add a Workout:</h3>

<div>
  <%= form_for @workout do |f| %>
    <div>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.submit %>
    </div>
  <% end %>
</div>
